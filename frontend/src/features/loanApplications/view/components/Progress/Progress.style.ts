import { makeStyles, colors } from 'utils/styles';

export const useStyles = makeStyles(() => ({
  timeLeftTitle: {
    color: colors.haiti,
  },

  timeLeftValue: {
    fontWeight: 500,
    color: colors.geraldine,
  },
}));
